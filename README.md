# Apache-VirtualHost-create-Script
shell script that helps set up a virtual host for both HTTP (port 80) and HTTPS (port 443) using Certbot for SSL. It prompts for each step, including whether to install SSL.
